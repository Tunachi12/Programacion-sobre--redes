# TP 4 - Agenda con delimitadores y encriptado

Proyecto Java de agenda en memoria con persistencia en `agenda.dat`.

La nota privada se guarda encriptada con AES y las modificaciones se persisten mediante un archivo temporal.
